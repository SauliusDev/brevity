---
description: Switch Brevity mode (auto/hard/off)
---

Switch Brevity mode using: /brevity auto|hard|off.

If no mode is specified, use auto. Auto defaults to the shortest useful answer, but follows explicit requests for longer reads, detail, examples, or formatting. Hard keeps the three-sentence cap; off removes the preference.
